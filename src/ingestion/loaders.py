# [SCAFFOLD — write this yourself]
# Document loaders: read raw files from corpus/raw/ and return
# a list of {text: str, metadata: dict} objects.
#
# What does a loader promise?
# What happens with malformed files, encoding errors, or empty documents?
